name: Debug Docker Username

on:
  push:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Display Docker Username
        run: echo "Docker username is: ${{ secrets.DOCKER_USERNAME }}"
